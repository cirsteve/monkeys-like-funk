(function (Backbone, _, $, undefined) {
    var __super__ = MNKY.PageView.prototype;
    
    MNKY.LabsPageView = MNKY.PageView.extend({
        template: MNKY.TMPL.monkeys_labs
    });
}(window.Backbone, window._, window.jQuery));
